<% layout('layouts/boilerplate') -%>

<br>
<div class="row justify-content-center">
    <div class="col-md-8">
        <div class="card">
            <img src="<%= listing.image.url %>" class="card-img-top" alt="<%= listing.title %> image" style="height: 25rem; object-fit: cover;">
            <div class="card-body">
                <h2 class="card-title"><%= listing.title %></h2>
                <h5 class="text-muted"><%= listing.location %>, <%= listing.country %></h5>
                <p class="card-text mt-3"><%= listing.description %></p>
                <p class="card-text fw-bold">₹ <%= listing.price.toLocaleString("en-IN") %> / night</p>

                <div class="d-flex justify-content-between mt-4">
                    <a href="/listings/<%= listing._id %>/edit" class="btn btn-outline-primary">Edit</a>

                    <form method="POST" action="/listings/<%= listing._id %>?_method=DELETE" onsubmit="return confirm('Are you sure you want to delete this listing?');">
                        <button class="btn btn-outline-danger">Delete</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
